// Auth.js
import React from 'react';

const Auth = () => {
  return (
    <div className="auth">
      <button>Log in</button>
      <button>Get a quote</button>
    </div>
  );
};

export default Auth;
